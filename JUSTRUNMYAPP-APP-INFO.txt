JustRunMy.App — app created 2026-04-02

Application ID: 16345
Dashboard name: Git r_Sg6q2 (rename in panel if rename failed)
Git remote (add username:password from panel → Settings → Git):
  https://justrunmy.app/git/r_Sg6q2

Health / keepAlive URL (HTTPS): https://a16345-a4fa.f.jrnm.app

Required environment variables (set in JustRunMy → App → Environment):
  DISCORD_BOT_TOKEN   — Discord bot token
  API_KEY             — Google Gemini API key
  DATABASE_URL        — Postgres connection string (e.g. Neon)
  TARGET_CHANNEL_IDS  — comma-separated Discord channel IDs

Already set: PORT=3000, NODE_ENV=production

Deploy updates (PowerShell):
  cd C:\Users\bafna\Desktop\discord-poll-bot-justrunmyapp-copy
  git add -A
  git commit -m "your message"
  git push -u https://YOUR_USER:YOUR_PASS@justrunmy.app/git/r_Sg6q2 HEAD:deploy

Use Control+Shift+` to open terminal in Cursor.
